cat > hlpflrecords/src/app/layout.tsx.bak << 'EOF'
import './globals.css'
import { Inter } from 'next/font/google'
import { Metadata } from 'next'
import StructuredData from '@/components/StructuredData'
import ChatbotAnalytics from '@/components/ChatbotAnalytics'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'HLPFL Records - World-Class Record Label',
  description: 'World-class record label dedicated to discovering, developing, and promoting exceptional musical talent. Elevating artists to global recognition.',
  metadataBase: new URL('https://hlpfl.org'),
  alternates: {
    canonical: '/',
  },
  openGraph: {
    title: 'HLPFL Records - World-Class Record Label',
    description: 'World-class record label dedicated to discovering, developing, and promoting exceptional musical talent.',
    url: 'https://hlpfl.org',
    siteName: 'HLPFL Records',
    images: [
      {
        url: '/og-image.jpg',
        width: 1200,
        height: 630,
        alt: 'HLPFL Records',
      },
    ],
    locale: 'en_US',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'HLPFL Records - World-Class Record Label',
    description: 'World-class record label dedicated to discovering, developing, and promoting exceptional musical talent.',
    images: ['/og-image.jpg'],
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-video-preview': -1,
      'max-image-preview': 'large',
      'max-snippet': -1,
    },
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <StructuredData />
        <ChatbotAnalytics />
        {children}
      </body>
    </html>
  )
}
EOF